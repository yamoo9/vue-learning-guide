// ------------------------------------------------------------------
// 프리미티브 타입 애너테이션 (Primitives Type Annotation, 유형 주석)
// ------------------------------------------------------------------

{
  // 문자 타입(string type)
  let message = `변수 타입은 변수 이름 뒤에 ':type'을 작성하는 것을 말하며 이를 "타입 에너테이션"이라 부릅니다.`;

  // 숫자 타입(number type)
  let currentYear = 2024;

  // 불리언 타입(boolean type)
  let isWatch = false;

  // 언디파인드 타입(undefined type)
  let notDefined = undefined;

  // 널 타입(null type)
  let empty = null;
}
